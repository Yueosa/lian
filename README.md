<div align="center">

# lian 🤖📦

paru / yay / pacman 的 TUI 前端，集成 AI 操作分析，适用于 Arch Linux 及其衍生发行版。

</div>

> 💡 **项目说明**
>
> 本项目是对 Claude AI 能力的探索实验. 这是作者第一次使用 Claude.
> - **项目框架** - 由 Claude Sonnet 4.5 生成
> - **Bug 修复与最终发布** - 由 Claude Opus 4.6 完成
>
> 感谢 Claude 在每个环节的支持！

## ✨ 特性

- 🎯 **智能检测** - 自动检测包管理器 (paru → yay → pacman)
- 🖥️ **精美 TUI** - 基于 ratatui 的终端界面，六大功能模块
- 📦 **系统更新** (Shift+U) - 流式输出、更新预览、AI 分析报告
- 🔽 **软件安装** (Shift+S) - 远程搜索、多选安装、依赖预览
- 🗑️ **软件卸载** (Shift+R) - 筛选已安装包、多选卸载、孤立依赖清理
- 🔍 **软件查询** (Shift+Q) - 本地/远程双面板搜索、包详情、文件列表
- ⚙️ **设置** (Shift+C) - TUI 内配置编辑，无需手动改文件
- 🤖 **AI 分析** - 支持对更新/安装/卸载操作的 AI 深度分析（默认 DeepSeek，兼容 OpenAI 格式）
- 🔍 **系统感知** - 自动检测系统环境（内核、GPU、桌面环境等），AI 分析更有针对性
- 💾 **自动存档** - 报告按操作类型和时间保存到 `~/.lian/pacman/YYYY/MM/DD/`

## 🚀 安装

### 前置要求

- Arch Linux（或衍生发行版，如 Manjaro、EndeavourOS、CachyOS 等）
- [DeepSeek API Key](https://platform.deepseek.com/api_keys) 或其他兼容 OpenAI 格式的 API

### 方法一：AUR 安装（推荐）

```bash
paru -S lian
# 或
yay -S lian
```

### 方法二：下载预编译版本

```bash
# 从 GitHub Releases 下载
# https://github.com/Yueosa/lian/releases

chmod +x lian_*_linux_x86_64
sudo mv lian_*_linux_x86_64 /usr/local/bin/lian
```

### 方法三：从源码编译

```bash
# 安装 Rust（如果没有）
paru -S rust
# 或: curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh

# 编译安装
git clone https://github.com/Yueosa/lian.git
cd lian
cargo build --release
sudo cp target/release/lian /usr/local/bin/
```

## ⚙️ 配置

首次启动时会自动生成配置文件 `~/.config/lian/config.toml`。

也可以在程序内通过 **Shift+C** 进入设置页面直接修改。

### 设置 API Key

两种方式任选其一（配置文件优先级更高）：

**方式一：配置文件**

在 `~/.config/lian/config.toml` 中设置 `api_key` 字段，或在 TUI 设置页面中编辑。

**方式二：环境变量**

```bash
echo 'export LIAN_AI_KEY="sk-your-key-here"' >> ~/.zshrc
source ~/.zshrc
```

### 配置文件说明

```toml
# AI 模型: "deepseek-chat" (快速) 或 "deepseek-reasoner" (深度分析)
model = "deepseek-reasoner"

# Temperature: 0.0-1.5，推荐 0.8
temperature = 0.8

# 报告保存目录
report_dir = "/home/your-username/.lian/pacman"

# AI API 地址（可选，默认 DeepSeek，兼容 OpenAI 格式）
# api_url = "https://api.deepseek.com/chat/completions"

# AI API Key（可选，优先级高于环境变量）
# api_key = "sk-your-key-here"

# HTTP 代理（可选，支持 http/https/socks5）
# proxy = "http://127.0.0.1:7890"

# AI 分析开关（按操作类型独立控制）
[ai]
update = true   # 系统更新后自动 AI 分析（默认开启）
install = false  # 安装软件包后 AI 分析（默认关闭）
remove = false   # 卸载软件包后 AI 分析（默认关闭）
```

## 📖 使用

```bash
lian
```

启动后进入仪表盘，通过快捷键切换功能模块。

### 全局快捷键

| 按键 | 功能 |
|------|------|
| `Shift+U` | 系统更新 (-Syu) |
| `Shift+S` | 安装软件包 (-S) |
| `Shift+R` | 卸载软件包 (-Rns) |
| `Shift+Q` | 查询软件包 |
| `Shift+C` | 设置 |
| `Ctrl+C` | 取消当前操作并退出 |
| `q` | 退出（仪表盘） |
| `Esc` | 返回上一级 |

### 模块内操作

| 按键 | 功能 |
|------|------|
| `↑` / `↓` | 滚动 / 选择 |
| `PgUp` / `PgDn` | 翻页 |
| `Enter` | 确认操作 |
| `Space` | 多选标记（安装/卸载） |
| `Tab` | 切换视图（日志 ↔ AI 分析 / 本地 ↔ 远程 / 文件 ↔ 目录） |

### 查看历史报告

```bash
# 查看最新报告
ls -t ~/.lian/pacman/*/*/*/*.md | head -1

# 查看今天的报告
ls ~/.lian/pacman/$(date +%Y/%m/%d)/
```

## 🔧 故障排除

### API 请求失败
1. 检查 API Key：`echo $LIAN_AI_KEY` 或在设置页面确认
2. 检查网络连接
3. 确认 API 服务状态

### 找不到包管理器
```bash
# 安装 paru（推荐）
sudo pacman -S paru
```

### 编译失败
```bash
rustup update stable
cargo clean && cargo build --release
```

## 📜 许可证

MIT License

## 🔗 链接

- **项目主页**: https://github.com/Yueosa/lian
- **问题反馈**: https://github.com/Yueosa/lian/issues
- **DeepSeek**: https://www.deepseek.com/
